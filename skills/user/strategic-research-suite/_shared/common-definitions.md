# Common Definitions - Strategic Research Suite

**全スキル共通の用語定義・概念・標準**

このファイルは、Strategic Research Suite内のすべてのスキルで使用される共通用語と概念を定義します。重複を避け、トークン消費を最小化するために、各スキルからこのファイルを参照します。

---

## 📚 基本用語定義

### 研究デザイン関連

**Panel Data（パネルデータ）**
- 定義: 複数の企業（i）を複数の時点（t）で観測したデータ
- 構造: (企業ID, 年, 変数1, 変数2, ...)
- 例: 1000社 × 10年 = 10,000観測

**Cross-sectional Data（横断面データ）**
- 定義: 複数の企業を単一時点で観測したデータ
- 構造: (企業ID, 変数1, 変数2, ...)
- 例: 2023年の1000社データ

**Time-series Data（時系列データ）**
- 定義: 単一企業を複数時点で観測したデータ
- 構造: (年, 変数1, 変数2, ...)
- 例: トヨタ自動車の1990-2023年データ

---

### 変数タイプ

**Dependent Variable（従属変数）**
- 別名: Outcome variable, Y
- 説明: 研究で説明したい対象変数
- 例: ROA, 売上成長率, イノベーション成果

**Independent Variable（独立変数）**
- 別名: Predictor, Explanatory variable, X
- 説明: 従属変数に影響を与えると仮説される変数
- 例: R&D投資, 組織構造, 市場シェア

**Control Variable（統制変数）**
- 説明: 交絡要因を統制するために含める変数
- 例: 企業規模, 業界, 年次

**Moderating Variable（調整変数）**
- 説明: X→Y関係の強さを変化させる変数
- 例: 環境の不確実性, 組織のスラック資源

**Mediating Variable（媒介変数）**
- 説明: X→Y関係のメカニズムを説明する中間変数
- 例: X(R&D投資) → M(組織学習) → Y(業績)

---

### 統計手法

**Fixed Effects (FE)**
- 別名: Within estimator
- 説明: 企業固有の時間不変な異質性を統制
- 数式: Y_it = β₁X_it + α_i + ε_it
- α_i: 企業固定効果

**Random Effects (RE)**
- 説明: 企業固有効果をランダムと仮定
- 使用条件: 企業固有効果とXが無相関

**Pooled OLS**
- 説明: パネル構造を無視した通常のOLS
- 問題: 企業固有効果・時間効果を無視

**Clustered Standard Errors**
- 説明: 企業内・時間内の相関を考慮した標準誤差
- 実装: cluster=firm_id または cluster=year

---

### 内生性問題

**Endogeneity（内生性）**
- 定義: 説明変数と誤差項が相関している状態
- 原因:
  1. Omitted variable bias（欠落変数バイアス）
  2. Simultaneity（同時性）
  3. Measurement error（測定誤差）

**Instrumental Variable (IV)**
- 説明: 内生変数と相関するが、誤差項と無相関な変数
- 条件:
  - Relevance: Cov(Z, X) ≠ 0
  - Exogeneity: Cov(Z, ε) = 0

**Selection Bias（選択バイアス）**
- 説明: サンプル選択が非ランダムな場合の��イアス
- 対策: Heckman selection model, PSM

---

## 📊 標準的な変数構築

### 財務パフォーマンス指標

**ROA (Return on Assets)**
```
ROA = 純利益 / 総資産
単位: %
標準化: 業界中央値で調整
```

**ROE (Return on Equity)**
```
ROE = 純利益 / 株主資本
単位: %
```

**Tobin's Q**
```
Tobin's Q = 企業の市場価値 / 資産の簿価
市場価値 = 株式時価総額 + 負債簿価
解釈: >1 → 市場は成長を期待
```

**Sales Growth**
```
売上成長率 = (売上_t - 売上_t-1) / 売上_t-1
単位: %
```

---

### イノベーション指標

**R&D Intensity**
```
R&D強度 = R&D支出 / 売上高
単位: %
業界標準: 製造業 3-5%, IT 10-15%
```

**Patent Count**
```
特許件数 = 出願特許数 or 登録特許数
ラグ: 通常 t+2年（審査期間考慮）
```

**Patent Citations**
```
特許被引用数 = 当該特許を引用した特許数
品質指標: 高被引用特許 = 高影響力
```

---

### ガバナンス指標

**Board Size**
```
取締役会規模 = 取締役人数
最適範囲: 7-12名（実証研究平均）
```

**Board Independence**
```
独立性 = 社外取締役数 / 総取締役数
単位: %
規制: 日本 1/3以上推奨（2021年改正）
```

**CEO Duality**
```
CEOと会長の兼任 = 1 (兼任), 0 (分離)
ダミー変数
```

---

### 組織構造指標

**Organizational Slack**
```
組織スラック = (流動資産 - 流動負債) / 売上高
単位: ratio
解釈: 余剰資源の大きさ
```

**Diversification**
```
多角化度 = 1 - Σ(売上シェア_i)²
Herfindahl Index の逆数
範囲: 0 (単一事業) ~ 1 (完全多角化)
```

**Vertical Integration**
```
垂直統合度 = 付加価値 / 売上高
単位: %
```

---

## 🔬 サンプル構築基準

### 標準的な除外基準

**財務異常値**
```python
# 極端な異常値の除外
df = df[
    (df['roa'] > -1) & (df['roa'] < 1) &  # ROA -100% ~ 100%
    (df['leverage'] >= 0) & (df['leverage'] < 10)  # レバレッジ 0~1000%
]

# Winsorization（1%と99%パーセンタイルで切断）
from scipy.stats.mstats import winsorize
df['roa_win'] = winsorize(df['roa'], limits=[0.01, 0.01])
```

**欠損値の扱い**
```python
# 主要変数の欠損を許容しない
required_vars = ['roa', 'rd_intensity', 'firm_size']
df = df.dropna(subset=required_vars)

# 説明用
print(f"元サンプル: {len(df_original)}")
print(f"除外: {len(df_original) - len(df)}")
print(f"最終サンプル: {len(df)}")
```

---

### 最小サンプルサイズ

**パネル回帰**
- 最小: N × T = 500観測（例: 100社 × 5年）
- 推奨: N × T = 2000観測（例: 200社 × 10年）
- 理想: N × T = 5000観測以上

**クロスセクション回帰**
- 最小: N = 100企業
- 推奨: N = 200企業
- 統計的検出力: N=300で中程度効果サイズ検出可能

---

## 📈 効果量の解釈

### 回帰係数の標準的解釈

**小さい効果**
- β < 0.10（標準化係数）
- 例: R² 増加 < 1%

**中程度の効果**
- 0.10 ≤ β < 0.30
- 例: R² 増加 1-5%

**大きい効果**
- β ≥ 0.30
- 例: R² 増加 > 5%

---

## 🎓 トップジャーナル基準

### データ品質基準

**SMJ (Strategic Management Journal)**
- サンプルサイズ: N×T ≥ 1000
- 観測期間: ≥ 5年
- 内生性対策: 必須（IV, PSM, FE等）
- Robustness checks: 3つ以上

**AMJ (Academy of Management Journal)**
- 理論的貢献: 強く要求
- 統計的検出力: Power ≥ 0.80
- 効果量報告: 必須
- 代替説明の検討: 必須

**OS (Organization Science)**
- データの新規性: 重視
- メカニズム検証: 媒介分析推奨
- 異質性分析: サブグループ分析推奨

---

## 🛠️ 共通Python設定

### 標準的なimport
```python
# データ処理
import pandas as pd
import numpy as np

# 統計分析
import statsmodels.api as sm
from linearmodels import PanelOLS, RandomEffects
from scipy import stats

# 可視化
import matplotlib.pyplot as plt
import seaborn as sns

# 設定
pd.set_option('display.max_columns', None)
pd.set_option('display.precision', 3)
sns.set_style('whitegrid')
```

### パネルデータの標準構造
```python
# MultiIndex設定
df = df.set_index(['firm_id', 'year'])

# 変数の標準化
from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()
df['rd_intensity_std'] = scaler.fit_transform(df[['rd_intensity']])
```

---

## 📝 報告基準

### 記述統計表（Table 1標準）
- 平均、標準偏差、最小値、最大値
- 相関係数行列（下三角）
- VIF（多重共線性チェック）

### 回帰結果表（Table 2以降標準）
- 係数、標準誤差（括弧内）
- 有意水準: * p<0.10, ** p<0.05, *** p<0.01
- R², Adjusted R²
- 観測数、企業数、年数
- Fixed Effects: Yes/No

---

## 🔗 参照方法

各スキルから以下のように参照：
```
詳細は _shared/common-definitions.md の「パネルデータ」セクション参照
```

---

**最終更新**: 2025-11-01  
**バージョン**: 4.0.0
